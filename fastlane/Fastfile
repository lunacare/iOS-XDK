default_platform :ios

platform :ios do
  skip_docs

  before_all do
    # ENV["SLACK_URL"] = "https://hooks.slack.com/services/..."
  end

  desc "Runs all the tests"
  lane :test do
    scan(
      scheme: "AtlasHost"
    ) 
    scan(
      scheme: "Programmatic"
    )
    scan(
      scheme: "Storyboard"
    )
  end
end
